import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  form: {
    margin: 20,
    display: 'flex'
  },
  button: {
    margin: 'auto 0',
    marginLeft: 10,
    height: 56
  },
  input: {}
});

export default useStyles;
